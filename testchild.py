# Adding a file in Child_Branch

condition = bool(34, 'fine')
child = 0.23
branch = int(3.433)
while condition:
  if child != branch:
    break
    print('Printed from Child Branch')
  else:
    continue
  
